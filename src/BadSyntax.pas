
program fullProg;
	var x
	y: integer;
	var z real;
	var someArray: array[1 5] of real; {inline comment}
	
	gcd (a: integer b : integer) : integer;
	begin
		if b = 0 then gcd:=a
		else gcd := gcd(b, a mod b)
	end;

	function aProc (w integer);
			w := x div y;
			while  w > 0 do
				begin
					w := w - 1;
					if x <> 0 then x := x + 1 else z := z/;
					if not((10 var 3) <= 3 then x:=2*5 else z:=5.2;
					if 10>=3 ; x:=2*5 else z=5.2;
					if 6>5  then x:=2*5 and else z:=5.2;
					if 6<5  then x:=2*5 else z:=5.E;
					if 10<=3 * then x:=2*5 else z:=5.2;
					x := div
				end	
	begin
		x := 17; ;
		y :=-
		y := gcd(x, y);
		x := aProc(x)
	end
